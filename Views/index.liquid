<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/skeleton.css">
    <link rel="stylesheet" href="/normalize.css">
    {% comment %} <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"> {% endcomment %}

    <title>Express Homework</title>
</head>
<body>
    <div class='container'>
        <h1>Budgtr ALL</h1>
        <h3>Bank Account: {{bankAccount}}</h3>
        <table>
            {%for item in allBudgetItems%}
        <tr>
            <td>{{item.date}}</td>
            <td><a href="/{{forloop.index0}}">{{item.name}}</a></td>
            <td>{{item.from}}</td>
            <td>{{item.amount}}</td>
            <td>{{item.tags}}</td>
        </tr>
        {%endfor%}
        </table>
        <button onclick=window.location.href="/new">Add New Item</button>
    </div>
    
</body>
</html>