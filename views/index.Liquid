<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="/public/normalize.css">
    <link rel="stylesheet" href="/public/skeleton.css"> -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css">
    <title>Index</title>
</head>

<body>
    <div class="container">

        <h1>Budgtr ALL</h1>

        {% if balance.amount < 0 %}
        <h3 style="background-color: red;">{{ balance.amount }}</h3>
        {% endif %}

        {% if balance.amount > 1000 %}
        <h3 style="background-color: green;">{{ balance.amount }}</h3>
        {% endif %}

        {% if balance.amount > 0 and balance.amount < 1000 %}
        <h3 style="background-color: white;">{{ balance.amount }}</h3>
        {% endif %}

        <table>
            {% for budgetItem in allBudgetItems %}
            <tr>
                <td>
                    {{ budgetItem.date }}
                </td>
                <td>
                    <a href="/budget/{{ forloop.index0 }}">{{ budgetItem.name }}</a>
                </td>
                <td>
                    {{ budgetItem.amount }}
                </td>
            </tr>
            {% endfor %}
        </table>

        <a href="/budget/new">
            <button>ADD NEW ITEM</button>
        </a>
    </div>
</body>

</html>