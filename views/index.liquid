<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <link rel="stylesheet" href="/normalize.css">
    <link rel="stylesheet" href="/skeleton.css">
    <style>
        .container{
            display:flex;
            flex-flow: column nowrap;
            justify-content: center;
            align-items: center;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Budgtr All</h1>
        {{ bankAccount }}
    
    <table>
        {% for budget in budgets%}
        <tr>
            <td>{{budget.date}}</td>
            <td><a href="http://localhost:3000/budgets/{{forloop.index0}}">{{budget.name}}</a></td>
            <td>{{budget.from}}</td>
            <td>{{budget.amount}}</td>
            <td>{{budget.tags | join: ', '}}</td>
            </tr>

        {% endfor %}
    </table>
    <a href="/budgets/new"><button>Add New Item</button></a>
</div>
</body>
</html>