<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css">
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css ">
    <title>Budget-Items</title>
</head>
<body>
    <div class="container">
    <h1>{{budget.name}}</h1>
    <button onclick="window.location.href='/budgets'">BACK</button>
    <p><strong>date</strong></p>
    <p class="value">{{budget.date}}</p>
    <p><strong>name</strong></p>
    <p class="value">{{budget.name}}</p>
    <p><strong>from</strong></p>
    <p class="value">{{budget.from}}</p>
    <p><strong>amount</strong></p>
    <p class="value">{{budget.amount}}</p>
    <p><strong>tags</strong></p>
    <p class="value">{{budget.tags}}</p>
    </div>
</body>
</html>